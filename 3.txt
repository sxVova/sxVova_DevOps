useradd -d /home/remontkd/name sxVovaster -g 100    // создаем пользователя, задаем ему ник, группу и домашнюю дерикторию
passwd sxVovaster 				    // активириуем пользователя, задавая ему пароль
su - sxVovaster					    // логинимся под новым пользователем

SHELL=/bin/bash					    // сron файл
MALTO=sxVovaster
0-59 * * * * /home/remontkd/name/mail/mail.sh

#!/bin/bash					    // mail.sh файл
echo "Hello from cron"

/sbin/ifconfig					    // просмотр сетевых интерфейсов

ping 8.8.8.8					    // пропингуем 8.8.8.8